def predict(model, data):
    """
    Uses the loaded model to make prediction.
    Currently returns dummy values.
    """

    return {
        "model": model,
        "radiation": 5.72,
        "risk": "Medium"
    }